## What changed

<!-- One paragraph: the user-visible behavior before and after. -->

## Checklist

- [ ] A test in `tests/<module>.test.ts` reproduces the bug or describes the feature, and fails without the change
- [ ] `npm test` and `npm run build` are green
- [ ] `README.md` is updated when the change is visible to users: flags, command names, profile fields, `.ocli/` layout, search behavior, supported spec features, which requests carry credentials or custom headers, spec loading and caching
- [ ] `examples/skill-ocli-api.md` and `skills/ocli-api/SKILL.md` still match the documented agent workflow
